import { Metadata } from "next";

export const metadata: Metadata = {
    title: {
        template: "%s | HBA SPORTS",
        default: "Onboarding"
    }
}

const ServerPage = async () => {};

export default ServerPage;