import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Login | HBA Sports",
    description: "Login to your HBA Sports scoring account."
}

const ServerPage = async () => {
    return
}

export default ServerPage